make a python file with concept name in AWS  ---> push your work to github

Indentation:  standard indentation requires standard Python code to use four spaces. but it also works with 1,2,3...spaces(shouldnt be in the same line )
print statement:
print("Im learning python")
Read the input:
input()
Variables and Types:
Object: An object is simply a collection of data (variables) and methods (functions) that act on those data. Python is completely object oriented.  You do not need to declare variables before using them, or declare their type. Every variable in Python is an object.

Types of Varibales:
1.Numbers: 
a. whole numbers(integers) b.Decimal numbers(float)
Defining an integer:           Defining a float:
intvar=5                            floatvar=7.0                 flaorvar=7                         flaotvar=flaot(7)       
print(5)                             print(flaotvar) (or)        print(flaot(flaotvar)) (or)     print(flaotvar)

o/p:7.0
